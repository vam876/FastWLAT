# 常见问题

FastWLAT 常见问题解答

## 安装问题

### Q: npm install 失败怎么办？

**A**: 尝试以下解决方案：

1. **清理缓存**
   ```bash
   npm cache clean --force
   rm -rf node_modules package-lock.json
   npm install
   ```

2. **使用国内镜像**
   ```bash
   npm config set registry https://registry.npmmirror.com
   npm install
   ```

3. **检查 Node.js 版本**
   ```bash
   node --version  # 应该 >= 18.0.0
   ```

### Q: 原生模块编译失败？

**A**: 安装构建工具：

**Windows**:
```bash
npm install --global windows-build-tools
```

**macOS**:
```bash
xcode-select --install
```

**Linux**:
```bash
sudo apt-get install build-essential
```

然后重新安装：
```bash
npm run postinstall
```

## 运行问题

### Q: 应用启动后白屏？

**A**: 可能的原因和解决方案：

1. **检查控制台错误**
   - 按 F12 打开开发者工具
   - 查看 Console 标签页的错误信息

2. **重新构建**
   ```bash
   npm run postinstall
   npm start
   ```

3. **检查文件完整性**
   ```bash
   npm run verify
   ```

### Q: GeoIP 功能不工作？

**A**: 检查数据库文件：

1. **确认文件存在**
   ```
   data/GeoLite2-City.mmdb
   data/GeoLite2-Country.mmdb
   data/GeoLite2-ASN.mmdb
   ```

2. **检查文件大小**
   - City: ~58 MB
   - Country: ~9 MB
   - ASN: ~10 MB

3. **查看控制台日志**
   ```
   [IPGeoLocationService] 数据库加载成功
   ```

### Q: 内存占用过高？

**A**: 优化建议：

1. **清理缓存**
   ```typescript
   // 在应用中执行
   await window.api.ipGeo.clearCache()
   ```

2. **减少数据量**
   - 分批导入日志
   - 定期清理旧数据

3. **关闭不用的功能**
   - 暂停实时监控
   - 减少图表数量

## 功能问题

### Q: 支持哪些日志格式？

**A**: 当前支持：

- Apache Combined
- Apache Common
- Nginx 默认格式
- IIS W3C 格式
- 自定义格式（通过配置）

### Q: 如何导入大文件？

**A**: 大文件处理建议：

1. **分批导入**
   - 将大文件分割成小文件
   - 每次导入 < 100MB

2. **使用流式处理**
   - 应用会自动使用 Worker 处理
   - 避免一次性加载全部数据

3. **监控进度**
   - 查看导入进度条
   - 等待处理完成

### Q: 数据存储在哪里？

**A**: 数据存储位置：

- **IndexedDB**: 浏览器本地数据库
- **位置**: 
  - Windows: `%APPDATA%\fastwlat`
  - macOS: `~/Library/Application Support/fastwlat`
  - Linux: `~/.config/fastwlat`

### Q: 如何备份数据？

**A**: 数据备份方法：

1. **导出数据**
   - 使用"导出"功能
   - 选择 JSON 格式
   - 保存到安全位置

2. **数据库备份**
   - 复制 IndexedDB 目录
   - 使用浏览器开发工具导出

## 性能问题

### Q: 解析速度慢？

**A**: 性能优化：

1. **检查系统资源**
   - CPU 使用率
   - 内存使用情况
   - 磁盘 I/O

2. **优化设置**
   - 减少 Worker 数量
   - 关闭实时分析
   - 禁用不需要的功能

3. **升级硬件**
   - 增加内存
   - 使用 SSD
   - 升级 CPU

### Q: 图表渲染卡顿？

**A**: 渲染优化：

1. **减少数据点**
   - 使用数据采样
   - 限制显示范围
   - 聚合数据

2. **降低刷新率**
   - 减少实时更新频率
   - 使用按需刷新

3. **简化图表**
   - 减少图表数量
   - 使用简单图表类型

## 构建问题

### Q: 构建失败？

**A**: 构建故障排除：

1. **检查依赖**
   ```bash
   npm run verify
   ```

2. **清理重建**
   ```bash
   rm -rf dist
   npm run build
   ```

3. **查看日志**
   - 检查构建输出
   - 查找错误信息

### Q: 打包后无法运行？

**A**: 检查项目：

1. **验证构建输出**
   ```bash
   npm run build:dir
   cd dist/win-unpacked
   ./FastWLAT.exe
   ```

2. **检查资源文件**
   - GeoIP 数据库
   - 配置文件
   - 依赖模块

3. **查看日志**
   - 运行应用
   - 查看控制台输出

## 数据问题

### Q: IP 地理位置不准确？

**A**: 可能的原因：

1. **数据库过期**
   - 下载最新的 GeoLite2 数据库
   - 替换 `data/` 目录中的文件
   - 重启应用

2. **IP 类型**
   - 私有 IP 无法定位
   - 某些 IP 段数据不完整

3. **数据库限制**
   - 免费版精度有限
   - 考虑使用商业版

### Q: 威胁检测误报？

**A**: 调整检测规则：

1. **查看规则配置**
   - 打开设置页面
   - 查看威胁规则

2. **调整阈值**
   - 降低敏感度
   - 自定义规则

3. **添加白名单**
   - 排除已知安全的 IP
   - 排除特定路径

## 其他问题

### Q: 如何更新应用？

**A**: 更新步骤：

1. **检查新版本**
   - 访问 GitHub Releases
   - 查看更新日志

2. **下载新版本**
   - 下载安装包
   - 或拉取最新代码

3. **安装更新**
   ```bash
   git pull
   npm install
   npm start
   ```

### Q: 如何卸载应用？

**A**: 卸载步骤：

1. **删除应用文件**
   - 删除安装目录
   - 或使用系统卸载程序

2. **清理数据**
   - 删除用户数据目录
   - 清理 IndexedDB

3. **清理配置**
   - 删除配置文件
   - 清理注册表（Windows）

### Q: 支持多语言吗？

**A**: 当前状态：

- ✅ 中文界面
- ⚠️ 英文界面（部分）
- 🔄 更多语言（规划中）


## 获取帮助

如果以上答案没有解决你的问题：

1. **搜索 Issues**
   - [GitHub Issues](https://github.com/yourusername/fastwlat/issues)
   - 可能已有相同问题

2. **提交新 Issue**
   - 描述问题
   - 提供错误信息
   - 说明环境信息

3. **加入讨论**
   - GitHub Discussions
   - 社区交流

4. **查看文档**
   - [完整文档](./README.md)
   - [故障排除](./troubleshooting.md)
